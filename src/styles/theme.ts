import { DefaultTheme } from "styled-components"; //DefaultThemes is used as an interface of props.theme

export const Theme: DefaultTheme = {

  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },

};